# Anthropic API key — get one at https://platform.claude.com/
# If unset, the app runs in "mock mode" with canned ideas so you can try the UI.
ANTHROPIC_API_KEY=

# Optional: override the model used for idea generation and preference inference
# SPARK_MODEL=claude-opus-4-8
